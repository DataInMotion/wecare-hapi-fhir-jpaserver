# Let's Encrypt Configuration Example
# Copy this to .env and update with your values

# Domain Configuration
DOMAIN=fhir.mi-jn.de
KEYCLOAK_HOSTNAME=auth.mi-jn.de

# Let's Encrypt Settings
LETSENCRYPT_EMAIL=your-email@example.com
LETSENCRYPT_TRIGGER_DIR=/var/lib/certbot

# Existing configuration...
# (Include all your existing .env variables here)
POSTGRES_DB=hapi-fhir
POSTGRES_USER=admin
POSTGRES_PASSWORD=secret
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=secret
CLIENT_ID=oauth2-proxy
CLIENT_SECRET=your-oauth2-proxy-client-secret
KEYCLOAK_REALM=hapi-fhir-dev
PROTOCOL=https
KEYCLOAK_PORT=8443
OAUTH2_PROXY_HOSTNAME=fhir.mi-jn.de
OAUTH2_PROXY_PORT=443
SCOPE=openid email profile
COOKIE_NAME=_oauth2_proxy
COOKIE_SECRET=your-cookie-secret